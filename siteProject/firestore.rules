rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /books/{bookId} {
      // Test rule - only require authentication for all operations
      allow read, write: if request.auth != null;
    }
  }
}